spring.application.name=school-bus-tracker-be
spring.config.import=optional:dotenv:.

<<<<<<< Updated upstream
# ===============================
# PostgreSQL (Docker / Prod)
# ===============================
spring.datasource.url=jdbc:postgresql://schoool-bus-tracker-db-yvettemanishimwe9-3e9f.b.aivencloud.com:18660/${DB_NAME}?sslmode=require
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Disable H2 completely
spring.h2.console.enabled=false

# HikariCP tuning (VERY IMPORTANT for managed DBs)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.connection-timeout=30000


# Server
server.port=8081
=======
# Database configuration (H2 for testing)
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.h2.console.enabled=true

# JPA/Hibernate configuration
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server configuration
server.port=8085
>>>>>>> Stashed changes

# JWT
app.jwt.secret=${JWT_SECRET}
app.jwt.expiration=${JWT_EXPIRATION}

<<<<<<< Updated upstream
# Default admin
app.admin.default.email=admin@school.com
app.admin.default.password=admin123
=======
# The amount of time (in milliseconds) before a generated JWT token
# expires. The default below (3600000) equals one hour. Override with
# `JWT_EXPIRATION` environment variable to change the validity period.
app.jwt.expiration=${JWT_EXPIRATION:3600000}

# Swagger UI
#
# Expose the Swagger UI at `/swagger-ui.html` instead of the default
# `/swagger-ui/index.html`. This property works with springdoc-openapi
# starter to generate interactive API documentation for your endpoints.
springdoc.swagger-ui.path=/swagger-ui.html

# Jackson configuration
spring.jackson.serialization.write-dates-as-timestamps=false
>>>>>>> Stashed changes
